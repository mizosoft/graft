syntax = "proto3";

package graft;

option go_package = "github.com/mizosoft/graft/pb;pb";

service Graft {
  rpc Append (AppendRequest) returns (AppendResponse);
}

message AppendRequest {
  repeated bytes cmds = 1;
}

message AppendResponse {
  int32 index = 1;
  int32 term = 2;
}
